extends layout


block content

    h1= title

    p Welcome to #{title}

    p Math.cos() is applied to #{value} is #{cos}

    p Math.asin() is applied to #{value1} is #{asin}

    p Math.asinh() is applied to #{value2} is #{asinh}